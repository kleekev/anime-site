import { useEffect, useState } from "react"

const UserCardAnimelist = () => {

    const [animelist, setAnimelist] = useState(null);

    useEffect(() => {

    }, [])

    return (
        <div className="animelist">
            Animelist
        </div>
    )
}

export default UserCardAnimelist